# macOS
.DS_Store
.AppleDouble
.LSOverride

# Icon must end with two carriage returns (\r) for the OS to recognize it
Icon


# Thumbnails
._*

# Files that might be created on a domestic NAS share
.DocumentRevisions-V100
.Spotlight-V100
.Trashes
.VolumeIcon.icns
.com.apple.timemachine.donotpresent

# Directories potentially created on external store
.TemporaryItems
.apdisk
